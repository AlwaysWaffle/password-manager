# Важно! Файл нужно переименовать в config.py

# Настройки проекта — меняем только здесь, в остальных файлах импортируем config

# Токен от @BotFather — бот ходит в Telegram API с этим ключом
BOT_TOKEN = "ВАШ_ТОКЕН"

# Файл SQLite на диске (создаётся сам при первом запуске)
DB_PATH = "passwords.db"

# После стольких неверных мастер-паролей — блокировка
MAX_FAILED_ATTEMPTS = 5
# На сколько минут блокируем
LOCKOUT_MINUTES = 10

# Как печатать время в логах, например 14:05:33
TIME_FORMAT = "%H:%M:%S"
